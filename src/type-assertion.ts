// let emni: any;

// // emni = "Next level web development";
// // ( emni as string ).toString()
// // <string>emni.toString()

// // emni = 222;
// // ( emni as number).toFixed()

// // function kgToGram(param: string | number): string | number | undefined {
// //   if (typeof param === "string") {
// //     const value = parseFloat(param) * 1000;
// //     return `The converted value is ${value}`;
// //   }
// //   if (typeof param === "number") {
// //     return `The converted value is ${param * 1000}`;
// //   }
// // }

// // const resultToBeNumber = kgToGram(1000) as number;
// // const resultToBeString = kgToGram("2000") as string;





// type CustomErrorType = {
//   message: string;
// };

// try {
// } catch (error) {
//   console.log((error as CustomErrorType).message);
// }
